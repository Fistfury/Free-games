export class Person {
    constructor (
        public name: string,
        public age: number,
        public email: string,
        public isActive: boolean,
        public address: string

    ) {}
}